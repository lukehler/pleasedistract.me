The assets directory can contain images, javascript libraries, icons, fonts, etc. Anything which should just be copied directly into the destination folder.

<% print 'Just seeing if the template tags are interpolated. If you see percentage signs, then it is not being templated while being copied.' %>